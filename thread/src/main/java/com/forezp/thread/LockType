java中锁的分类

1. 线程要不要锁住共享资源
    锁住 -> 悲观锁
    不锁住 -> 乐观锁
2. 多线程是否能共享一把锁
    能共享 -> 共享锁
    不能共享 -> 独占锁
3. 多线程竞争时是否排队
    排队 -> 公平锁
    先尝试插队，插队失败再排队 -> 非公平锁
4. 同一个线程是否能重复获取同一把锁
    能 -> 可重入锁
    不能 -> 不可重入锁
5. 是否可中断
    可 -> 可中断锁
    不可 -> 不可中断锁
6. 等锁的过程
    自旋锁
    阻塞 -> 非自旋锁